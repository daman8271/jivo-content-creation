# ❌ STATUS: FAILED / NOT GOOD (rejected attempt — kept on record)

`mango-v1-480p.mp4` — first attempt at a fast-cut CGI product reel for **Jivo Wheatgrass Mango**, modeled on `example.mp4` (VAZU Dragon Lychee).

**Marked by the user as a failure / not good.** Saved for reference only — **do not use**.

## Why it failed
- **Logo drift:** the JIVO wordmark distorts to "LiiVO" / "Liivg" as the bottle rotates (Seedance label drift on rotation).
- **Wrong structure:** it's a single continuous dynamic shot — it does **not** match the example's hard-cut, multi-beat pacing/cuts.

## Next direction
- Build the exact **hard-cut, multi-beat** version (macro → ripple → fruit splash → reveal → ice → juice-wrap → hero), assembled from per-beat clips.
- Keep the product **front-locked** so the JIVO logo stays accurate.
- Resolution ≤ 720p.

## Files
- `mango-v1-480p.mp4` — the failed reel (kept)
- `hero-A.png` / `hero-B.png` — hero stills used as the animation anchor
